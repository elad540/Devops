version: '3.8'
services:
  jenkins:
    container_name: jenkins
    restart: always
    image: jenkins/jenkins:jdk11
    ports:
      - "8080:8080"
      - "5151:5151"
    volumes:
      - jenkins-home:/var/jenkins_home

volumes:
  jenkins-home: